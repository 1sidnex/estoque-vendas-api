# Opcional. Se não for definida, o front usa /api e o proxy do Vite
# encaminha as requisições para http://localhost:3000 (veja vite.config.js).
# Use esta variável quando a API estiver em outro endereço, por exemplo:
# VITE_API_URL=http://localhost:3000/api
